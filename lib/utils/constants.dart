import 'package:flutter/material.dart';

const double webConstraint = 600;
Color primaryColor = Colors.blueAccent.shade700;
EdgeInsetsGeometry padding =
    const EdgeInsets.symmetric(horizontal: 16.0, vertical: 40);
